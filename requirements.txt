# Core backend (required)
fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
pydantic==2.9.2
python-multipart==0.0.9

# Optional — the app works fine without these (falls back to demo data)
# yfinance==0.2.43     # uncomment for live market data
# pypdf==4.3.1         # uncomment to ingest real PDF documents (.txt works without it)
