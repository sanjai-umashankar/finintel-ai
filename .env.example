# Nothing here is required for the demo — the app runs fully on demo data
# with zero configuration. These are optional upgrade points.

# Enables real embeddings/LLM reasoning in a future upgrade of the agents
# (not used by the current rule-based agent implementations).
# OPENAI_API_KEY=

# If yfinance is installed, live NSE prices are used automatically for
# RELIANCE, TCS and HDFCBANK; no key needed for yfinance itself.
